# pfoliokitr 0.1.0

* Initial CRAN submission.
* Initial release: `tckr_data()`, `log_ret()`, `aligned_returns()`,
  `metrics()`, `risk_metrics()`, `bootstrap_returns()`,
  `relative_metrics()`, `combine_metrics()`, print and data.frame
  methods for `portfolio_metrics`.
  
