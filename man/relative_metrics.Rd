% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{relative_metrics}
\alias{relative_metrics}
\title{Compute metrics relative to a benchmark}
\usage{
relative_metrics(ret, bench, rf)
}
\arguments{
\item{ret, bench}{xts objects of daily log returns (need not be
pre-aligned — this function aligns them internally via
\code{aligned_returns()}).}

\item{rf}{Daily risk-free rate.}
}
\value{
An object of class \code{portfolio_metrics} with \code{beta} and
annualised \code{alpha} (\%).
}
\description{
Compute metrics relative to a benchmark
}
\examples{
dates <- as.Date("2024-01-01") + 0:251
bench <- xts::xts(rnorm(252, 0.0005, 0.01), order.by = dates)
ret <- xts::xts(1.2 * as.numeric(bench) + rnorm(252, 0, 0.005), order.by = dates)
relative_metrics(ret, bench, 0.05)
}
