% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{bootstrap_ret}
\alias{bootstrap_ret}
\title{Simulate multi-day returns via block bootstrap}
\usage{
bootstrap_ret(returns, block = 10, n_sim = 10000)
}
\arguments{
\item{returns}{Numeric vector or xts object of daily log returns.}

\item{block}{Number of consecutive days to sum per simulated draw.
Default 10.}

\item{n_sim}{Number of simulated draws. Default 10000.}
}
\value{
A numeric vector of length \code{n_sim}, each a simulated \code{block}-day
log return.
}
\description{
Resamples \code{n_sim} random starting points and sums \code{block} consecutive
daily log returns from each, producing simulated \code{block}-day returns.
Since log returns are additive, this preserves some of the day-to-day
correlation structure that a plain (single-day) resample would destroy.
}
\examples{
daily_ret <- rnorm(100, 0, 0.02)
sim  <- bootstrap_ret(daily_ret, 10,1000)
risk_metrics(sim, 0.95, "historical")

}
