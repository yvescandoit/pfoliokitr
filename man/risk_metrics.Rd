% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{risk_metrics}
\alias{risk_metrics}
\title{Compute Value at Risk and Conditional VaR(ES) for a return series}
\usage{
risk_metrics(ret, p = 0.95, method = c("historical", "gaussian", "modified"))
}
\arguments{
\item{ret}{An xts object of daily log returns.}

\item{p}{Confidence level. Default 0.95.}

\item{method}{Distribution assumption: "historical" (empirical, no
distributional assumption), "gaussian" (assumes normal returns), or
"modified" (Cornish-Fisher, adjusts for skew/kurtosis).}
}
\value{
An object of class \code{portfolio_metrics} with \code{var} and \code{cvar},
both expressed as percentages.
}
\description{
Compute Value at Risk and Conditional VaR(ES) for a return series
}
\examples{
dates <- as.Date("2024-01-01") + 0:99
ret   <- xts::xts(rnorm(100, 0, 0.02), order.by = dates)
risk_metrics(ret, 0.95, method = "historical")
risk_metrics(ret, 0.95, method = "gaussian")

}
